# Copy this file to `.env` and fill in real values.
# The `.env` file is loaded automatically by the judge script.

OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional. Overridden by the --model flag if provided on the command line.
OPENAI_MODEL=gpt-4o-mini
